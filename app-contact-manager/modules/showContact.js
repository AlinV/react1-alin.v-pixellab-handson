export const otherMessage = (contact, pet) => {
  const container = document.querySelector('.notification-bar');
  const { name: contactName, surname: contactSurname } = contact;
  const { name: petName } = pet;
};
